CREATE TABLE ouvidoria3 (
   codigo int AUTO_INCREMENT,
    titulo varchar (100),
    descricao varchar (1000),
    tipo varchar (100),
    autor varchar (100),
    primary key (codigo)
);

select* from ouvidoria3
